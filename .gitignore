.gradle/
build/
app/build/
local.properties
.idea/
*.iml
.kotlin/
captures/
.externalNativeBuild/
.cxx/

# Device screenshots - debugging output, not source
tools/shots/

*.jks
*.keystore
*.p12
keystore.properties
key.properties
release-key*

# One-time signing setup, deliberately not committed. It holds no secrets, but a script
# that walks the key material has no business being a permanent fixture - and its absence
# is a reminder that this is a once-ever operation. See docs/17-releases.md.
tools/setup-signing.ps1
